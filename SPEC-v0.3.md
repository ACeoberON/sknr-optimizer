# sknr-optimizer 스펙 v0.3 — 세팅 최적화기 (현재 스탯 기준)

v0.2 시뮬레이터 위에 **최적화기**를 얹는다.
사용자가 현재 마을 스탯만 입력하면, 같은 장비 예산 안에서 치확·약확·치피를 어떻게 나눠야
베스트1 기대값이 최대인지, 그리고 지금 대비 어떤 줄을 바꾸면 얼마나 이득인지 알려준다.

---

## 1. 장비 규칙 (게임 사실)

| 항목 | 내용 |
|---|---|
| 슬롯 | 무기 2, 방어구 2, 장신구 1, 전용장비 1 |
| 무기 주옵 | 치확 24 / 치피 36 / 약확 28 중 하나 |
| 방어구 주옵 | 모공 28 (배분 무관) |
| 부옵 | 장비당 4줄, **부옵끼리는 종류 중복 불가**, 주옵과는 중복 가능 |
| 부옵 1줄 기본값 | 치확 4 / 치피 6 / 약확 5 / 모공 5% / 깡공 50 / 그 외(방%·생% 등 잡옵) |
| 강화 | 15강까지 3강마다 1회 = 장비당 5회. 4줄 중 **무작위** 1줄에 기본값 1개분 추가 |
| 재설정 | 강화 배분 재설정, 부옵 종류 변경 모두 **무작위**. 종류 변경 시 1줄 고정 가능(비용 2배) |
| 장신구 | 토벌(보스 피해 +12%) 고정 + 세공 1개: 치확 +10 / 약확 +12 / 공성 |
| 전용장비 | 피해 증폭 등 상수 → 무시 |
| 세트 | 복수자 등 딜 상수 → 무시 (같은 세트 유지는 사용자 몫) |

**단위**: 부옵 1줄 또는 강화 1회 = "1단위". 치확 1단위 = 4, 치피 1단위 = 6, 약확 1단위 = 5.
장신구 세공(치확 +10, 약확 +12)은 전투 전용 (마을 스탯 미표시).
공성 세공: 1~3인기 딜 표기 +10%, **실효 ×1.04 가정** (커뮤니티 실측: 치피 300 근처에서 치피 +12 상당). 미검증 표시.

---

## 2. 입력

딜러별로 (지크·비스킷 등 딜 비중 1% 미만 캐릭터는 입력 생략 가능):
```ts
type CurrentSetup = {
  charId: string;
  village: { crit: number; critDmg: number; weakRate: number }; // 현재 마을 스탯
  weaponMains: [MainStat, MainStat];   // "crit" | "critDmg" | "weakRate"
  ringCarve: "crit" | "weak" | "siege";
};
```
기본 스탯(장비 미착용 마을값)은 v0.1 `users.json`의 `baseStats` 사용.

---

## 3. 예산 산출

```
부옵 치확 = 마을 치확 − 기본 치확 − 무기 주옵 치확 합
부옵 치피 = 마을 치피 − 기본 치피 − 무기 주옵 치피 합
부옵 약확 = 마을 약확 − 기본 약확 − 무기 주옵 약확 합

예산 B = 부옵 치확/4 + 부옵 치피/6 + 부옵 약확/5   (단위, 소수 허용)
```
- 잡옵·공% 강화·속공 줄은 예산에 안 잡힌다 → 장비 완성도가 자동 반영됨
- 속공 등 공략상 필요한 줄은 이미 예산 밖이므로 별도 처리 불필요

---

## 4. 탐색

### 4.1 후보 공간 (딜러 1명당)
- 무기 주옵 조합: {치확, 치피, 약확} 중복조합 2개 = 6가지
- 장신구 세공: 3가지
- 예산 B를 (치확 u_c, 약확 u_w, 치피 B − u_c − u_w)로 0.5단위 간격 분할
- 제약: 각 스탯 단위 ≤ 4줄 + 강화 20회 = 24 (사실상 비구속)
- 전투 스탯 = 기본 + 주옵 + 부옵 + 세공 + 전투 버프, 치확·약확은 100에서 자름

### 4.2 평가 (빠른 해석 계산)
시뮬레이션 대신 히트별 해석식으로 평균·분산을 구한다 (v0.2 히트 목록·가중치 재사용):
```
히트 h의 배율 X_h = 치명항 × 약공항 (독립)
E[치명항] = 1 + p_c (cd_h/100 − 1),  Var = p_c(1−p_c)(cd_h/100 − 1)²
E[약공항] = 1 + 0.3 p_w,              Var = p_w(1−p_w)(0.3)²
E[X] = E[치명]E[약공],  Var[X] = E[치명²]E[약공²] − E[X]²
캐릭터 딜: μ_c = scale_c Σ w_h E[X_h],  σ²_c = scale_c² Σ w_h² Var[X_h]
팀 목적함수: J = Σ μ_c + 1.163 √(Σ σ²_c)     // 베스트1/5 정규 근사
```
- scale_c: v0.2 캘리브레이션 값 사용
- 딜러 간 결합은 √(Σσ²) 뿐 → 딜러별 최적화를 번갈아 2~3회 반복(좌표 하강)하면 수렴

### 4.3 확인
해석 계산 상위 5개 후보를 v0.2 시뮬레이터(20,000판)로 재평가해 베스트1/5 기대값 확정.

---

## 5. 출력

### 5.1 목표
딜러별: 목표 마을 스탯(치확·치피·약확), 무기 주옵 2개, 장신구 세공, 예상 베스트1 기대값과 현재 대비 증감(%)

### 5.2 줄당 가치 (재설정 가이드)
현재 세팅에서 각 스탯 **1줄(부옵 기본값 1개)**을 더했을 때의 J 증가율:
```
치확 +4 → +x.x%   치피 +6 → +y.y%   약확 +5 → +z.z% (상한 초과 시 0)
```
사용자는 이 순위대로 잡옵·공% 강화 줄을 재설정하면 된다. 재설정이 무작위이므로 정확한 조작 지시는 내지 않는다.

### 5.3 경고
- 상한 초과 낭비: "전투 약확 108 → 약 1.6줄 낭비"
- 목표 대비 부족·초과 줄 수

---

## 6. v0.2 잔여 수정

- **평타 배분**: `8 + 0.5 가중` 대신 결정적 배분.
  총 턴 T, 유닛 수 N → 모든 유닛 ⌊T/N⌋회, 속공 내림차순 앞 (T mod N)명 +1회.
  동속이면 진형 순서, 보스 속공은 콘텐츠 DB에 `enemySpeeds` 추가(모르면 아군 뒤로 가정).

---

## 7. 이번 작업 범위 (v0.3)

1. `src/gear/budget.ts`: 3장 예산 산출
2. `src/engine/analytic.ts`: 4.2 해석 평가 (+ 시뮬레이터와 평균·분산이 1% 이내로 일치하는지 테스트)
3. `src/gear/optimize.ts`: 4.1 탐색 + 4.3 확인
4. `src/gear/marginal.ts`: 5.2 줄당 가치
5. 6장 평타 배분 수정 (v0.2 테스트 통과 유지)
6. 화면: 딜러별 현재 스탯 입력 폼 → 목표·줄당 가치·경고 표시
7. 테스트
   - 해석 평가 vs 시뮬레이터: 세팅 1·2 모두 평균·표준편차 1% 이내
   - 예산 산출: 세팅 2 라이언(마을 치확 59, 치피 312, 약확 20, 주옵은 입력값) 역산 후 재조합 시 원래 스탯 복원
   - 최적화 결과: 치확·약확 전투값 ≤ 100 + 1단위 (낭비 한도)
8. CLAUDE.md에 1장 장비 규칙 추가

## 8. 미결 사항
- 공성 세공 실효값 (현재 가정 ×1.04)
- 인벤토리 모드 (보유 장비 입력 → 5명 배치·재설정 우선순위) — 다음 단계
- 레이첼 흔들림, 타카 디버프 곡선 (v0.2 미결 유지)
