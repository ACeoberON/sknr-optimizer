# sknr-optimizer 스펙 v0.3.2 — 장비 구조 기반 최적화

v0.3의 "예산 단위 자유 배분"은 불가능한 목표를 낼 수 있었다
(예: 라이언 목표 치피 317 > 물리적 최대 294). 장비 구조를 그대로 모델링하도록 바꾼다.

---

## 1. 장비 구조 (종결 기준)

딜러의 무기 2 + 방어구 2, 총 4개 장비는 모두 다음 4줄 구성을 가정한다.
부옵끼리는 종류 중복 불가이므로 이 구성이 최선이다.

| 칸 | 부옵 | 비고 |
|---|---|---|
| 1 | 치확 4 | |
| 2 | 치피 6 | |
| 3 | 모공 5% | 모든 후보에 동일 → 상수, 계산 제외 |
| 4 | 약확 5 **또는** 깡공 50 | 전투 약확이 기본+버프만으로 100 이상이면 깡공(상수, 계산 제외) |

- 주옵: 무기 2개는 각각 치확 24 / 치피 36 / 약확 28 중 선택, 방어구 2개는 모공 28(상수)
- 강화: 장비당 5회 × 4 = **20회**. 각 1회 = 해당 줄 기본값 1개분 추가
- 결과적으로 부옵 기여 = 치확 4×(4 + k_c), 치피 6×(4 + k_d), 약확 5×(4 + k_w) (4칸이 약확일 때)
- k_c + k_d + k_w = 20 − 손실 강화 수

**손실 강화 수**: 강화가 모공·깡공에 붙은 횟수. 사용자 입력(기본 0 = 종결).

---

## 2. 입력 (캐릭터별)

```ts
type CharInput = {
  charId: string;
  optimize: boolean;            // 콘텐츠 DB lineup의 optimize 플래그. false면 폼 비활성화
  current: {                    // 현재 세팅 (비교·현황용)
    village: { crit: number; critDmg: number; weakRate: number };
    weaponMains: [MainStat, MainStat];
    ringCarve: "crit" | "weak" | "siege" | "survival";
  };
  lostUpgrades: number;         // 0~20, 기본 0
  reservedSpeedLines?: number;  // 공략상 속공 부옵이 필요한 줄 수 (해당 칸을 4칸에서 대체), 기본 0
};
```
- 기본 스탯(장비 미착용 마을값)은 `users.json`의 `baseStats`
- `reservedSpeedLines`: 속공 줄은 4번째 칸(약확/깡공)을 대체한다고 가정

---

## 3. 탐색 (딜러 1명당)

후보:
- 무기 주옵 조합 6가지 (중복조합)
- 반지 세공 4가지 (치확 / 약확 / 공성 ×1.04 가정 / 생존)
- 4번째 칸: 약확 or 깡공 (1장 규칙으로 자동 결정, 단 약확이 부족한 캐릭터는 둘 다 평가)
- 강화 배분 (k_c, k_d, k_w), 합 = 20 − lostUpgrades, 모두 정수 ≥ 0

평가는 v0.3의 해석 목적함수 그대로:
`J = Σ μ_c + 1.163 √(Σ σ²_c)`, 딜러별 좌표 하강. 상위 5개는 시뮬레이터로 확인.
경우의 수: 6 × 4 × 2 × (약 231) ≈ 1.1만 / 딜러 → 전수 탐색.

---

## 4. 출력 (캐릭터별)

### 4.1 레시피
```
무기 1: 주옵 치확 24 | 부옵 치확 · 치피(+5강) · 모공 · 깡공
무기 2: 주옵 치확 24 | 부옵 치확 · 치피(+5강) · 모공 · 깡공
방어구 1: 주옵 모공 28 | 부옵 치확(+1강) · 치피(+4강) · 모공 · 깡공
방어구 2: 주옵 모공 28 | 부옵 치확 · 치피(+5강) · 모공 · 깡공
장신구: 토벌 + 치확 세공
강화 합계: 치확 1 / 치피 19
```
강화는 무작위이므로 **장비별 배분은 예시**이고, 핵심은 **합계**(치확 k_c회, 치피 k_d회)라고 표시.

### 4.2 목표 스탯
목표 마을 치확·치피·약확, 전투 치확·약확, 예상 베스트1 기대값

### 4.3 현재 대비
- 현재 입력 스탯으로 계산한 J와 목표 J의 차이(%)
- 현재 치확·약확이 목표보다 많거나 적은 줄 수
- 줄당 가치 (v0.3 5.2 유지)

### 4.4 경고
- 상한 초과: **장비로 줄일 수 있는 초과분만** (기본 스탯 + 전투 버프로 넘는 부분 제외)

---

## 5. 화면

- 콘텐츠 편성 5명을 진형 순서로 모두 표시
- `optimize: false` 캐릭터(금요일: 비스킷)는 폼 회색 비활성화 + "생존 세팅 (최적화 제외)"
- 지크를 최적화 대상에 추가: 히트는 평타만(가중치 동일), 전투 약확 버프 +27
- 입력 기본값 = 실제 현재 세팅:

| 캐릭터 | 마을 치확/치피/약확 | 무기 주옵 | 반지 |
|---|---|---|---|
| 라이언 | 100 / 258 / 20 | 치확 + 치피 | 공성 |
| 타카 | 99 / 270 / 20 | 치확 + 치피 | 공성 |
| 레이첼 | 93 / 246 / 45 | 치확 + 치피 | 치확 |
| 지크 | 99 / 270 / 5 | 치확 + 치피 | 생존 |
| 비스킷 | (비활성화) | | |

(라이언·타카·지크 무기 주옵은 미확인 기본값, 사용자가 화면에서 수정)

---

## 6. 작업 범위

1. 1~3장 모델로 `optimize.ts` 교체 (v0.3 예산 방식 제거, 예산 산출은 현황 표시용으로만 유지)
2. 4장 출력 (레시피 포함)
3. 5장 화면 변경
4. 테스트
   - 모든 추천의 마을 치피 ≤ 150 + 주옵 치피 + 6×(4 + 20) (물리적 상한)
   - lostUpgrades 증가 시 J 단조 감소
   - 약확이 버프로 100 이상인 캐릭터(라이언·타카)는 4번째 칸 = 깡공
   - 비스킷 폼 비활성화
   - v0.1~v0.3 기존 테스트 유지(v0.3 예산 최적화 테스트는 새 모델 기준으로 갱신)
5. CLAUDE.md에 1장 장비 구조 추가
