# CLAUDE.md

## 프로젝트 개요

sknr-optimizer는 장비 조합을 스탯 가중치 기준으로 최적화하는 웹 애플리케이션이다.
Vite + React + TypeScript로 구성하며, 테스트는 Vitest를 사용한다.
`main` 브랜치에 push하면 GitHub Actions가 GitHub Pages로 자동 배포한다.

## 폴더 역할

- `src/data` — 장비/스탯 등 정적 데이터 정의
- `src/engine` — 최적화 계산 로직 (순수 함수)
- `src/gear` — 장비 도메인 타입과 모델
- `src/ui` — React 컴포넌트 (화면)
- `tests` — Vitest 테스트

## 도메인 규칙 요약 (SPEC 1장)

- **턴 구조**: 1턴 = 평타 1회. 전 유닛이 속공 내림차순으로 라운드로빈. 유닛당 평타 = 총 턴 ÷ 유닛 수(아군+적, 소수 허용). 스킬은 턴을 소모하지 않으며 순서는 사용자가 입력.
- **딜 판정(히트 단위, 독립)**: 치명 확률=실효 치확·배율=치피/100, 약공 확률=실효 약확·배율=1.3. 둘은 동시 발동(곱연산). 모든 스킬·평타가 판정 대상이며 예외는 `canCrit: false`.
- **실효 스탯** = 기본(마을값, 초월 반영) + 장비 + 전투 버프. 치확·약확 상한 100, 초과분은 낭비. 버프는 대상 자기 평타 턴 기준 감소하며 기본 `uptime: "always"`.
- **모델 제외**: 공격력·피증·방깎·받피증, 쿨감·속공, 비중 작은 스킬 고유 치피, 도트딜, 조건부 추가타.
- **부옵**: 공% 대신 치피 우선(치확 낮은 캐릭터는 재검토).

## 실측 확정 규칙 (SPEC v0.2 1장)

금요일 공성전 연습 10판으로 검증됨.

- 치명·약공은 **히트마다·대상마다 독립 판정** (확정).
- 치명 배율 = 치피 / 100, 약공 배율 = 1.3 (확정).
- 치명·약공 외 랜덤 요소는 무시 (치확·약확 100 이상에서 딜 완전 동일).
- 딜 ∝ 공격력 (실용상 통과).
- 레이첼 패시브 약확 +27은 **자신 포함** 전원 대상 (확정).

## 시뮬레이터 (SPEC v0.2 3장)

- `src/engine/rng.ts` — 시드 고정 난수 (mulberry32).
- `src/engine/simulate.ts` — 몬테카를로 한 판/N판 시뮬레이션. 히트 = 스킬 순서 × 대상 수 × hitsPerTarget + 평타. 평타 수가 소수(8.5)면 정수부 온전 히트 + 소수부 가중 히트로 **결정적** 처리(원 스펙의 판별 8/9 랜덤 대신 — 결정성 테스트 충족용, 기대값 동일).
- `src/engine/calibrate.ts` — 캐릭터별 스케일 = 실측 평균 / 시뮬 평균(보정 전). 배분이 바뀌어도 고정.
- 히트 크기(hitWeights)는 콘텐츠 DB에 편성별로 둔다.

## 장비 규칙 (SPEC v0.3 1장)

- **슬롯**: 무기 2, 방어구 2, 장신구 1, 전용장비 1.
- **무기 주옵**: 치확 24 / 치피 36 / 약확 28 중 하나. **방어구 주옵**: 모공 28(배분 무관).
- **부옵**: 장비당 4줄, 부옵끼리 종류 중복 불가. 1줄 기본값 = 치확 4 / 치피 6 / 약확 5 / 모공 5% / 깡공 50.
- **단위**: 부옵 1줄 또는 강화 1회 = 1단위. 치확 1단위=4, 치피=6, 약확=5.
- **강화**: 15강까지 장비당 5회, 4줄 중 무작위 1줄에 기본값 1개분 추가.
- **재설정**: 강화 배분·부옵 종류 변경 모두 무작위(→ 정확한 조작 지시는 내지 않음).
- **장신구 세공**: 치확 +10 / 약확 +12 / 공성(딜 표기 +10%, 실효 ×1.04 가정, 미검증). 세공은 전투 전용(마을 미표시).
- **전용장비·세트**: 딜 상수 → 무시.

## 장비 구조 (SPEC v0.3.2 1장)

종결 기준으로 딜러의 무기 2 + 방어구 2 = 4장비, 각 4줄:
- 1칸 치확 4 · 2칸 치피 6 · 3칸 모공 5%(상수 제외) · 4칸 **약확 5 또는 깡공 50**.
- 4번째 칸: 기본 약확+버프+약확 4줄(20)로 100 이상이면 깡공(약확 낭비), 아니면 약확.
- 주옵: 무기 2개 치확 24/치피 36/약확 28, 방어구 2개 모공 28(상수).
- 부옵 기여 = 치확 4×(4+k_c), 치피 6×(4+k_d), 약확 5×(줄수+k_w). 강화 k 합 = 20 − 손실 강화.

## 최적화기 (SPEC v0.3 → v0.3.2)

- `src/gear/optimize.ts` — **장비 구조 기반 전수 탐색**: 무기 주옵 6조합 × 세공 4가지 × 4번째 칸(약확/깡공) × 강화 배분(k_c+k_d+k_w=20−손실). 목적함수 μ + 1.163σ, 상위 5개를 시뮬레이터로 확정. (v0.3의 자유 예산 배분은 제거 — 물리적으로 불가능한 목표를 막음.)
- `src/engine/analytic.ts` — 히트별 해석식 평균·분산 (시뮬레이터와 1% 이내 일치).
- `src/gear/budget.ts` — 예산 B (마을 − 기본 − 무기 주옵 역산). **현황 표시용으로만** 유지.
- `src/gear/marginal.ts` — 줄당 가치 (스탯 1줄 추가 시 J 증가율).
- 평타 배분(v0.3 6장): 결정적 — 모든 유닛 ⌊T/N⌋회, 속공 상위 (T mod N)명 +1회.

## 데이터 계층

- 캐릭터 DB (`src/data/characters.json`) — 누구에게나 같은 정보 (관리자)
- 콘텐츠 DB (`src/data/contents/*.json`) — 콘텐츠·편성별 정보 (관리자)
- 사용자 데이터 (`src/data/users.json`) — 사람마다 다른 기본 스탯·장비·프리셋 (사용자)

## 규칙

(추후 추가 예정)
