# sknr-optimizer 스펙 v0.2

v0.1(데이터 계층·판정 수)에 이어 **몬테카를로 시뮬레이터**를 구현한다.
v0.1의 1장(도메인 규칙)·2장(스키마)은 유지하고, 아래 변경·추가 사항만 반영한다.

---

## 1. 실측으로 확정된 규칙 (v0.1 1.2 갱신)

금요일 공성전 연습 10판(세팅 1: 5판, 세팅 2: 5판)으로 검증함.

| 규칙 | 상태 | 근거 |
|---|---|---|
| 치명·약공은 **히트마다, 대상마다 독립 판정** | 확정 | 스킬 1회에 대상별 치명 여부가 다르게 뜸 (육안 확인) |
| 치명 배율 = 치피 / 100 | 확정 | 세팅 2 평균 예측 오차 라이언 +2.5%, 타카 −0.7% |
| 약공 배율 = 1.3 | 확정 | 레이첼 아랫스킬 45,446 → 59,106 (비율 1.300) |
| 치명·약공 외 랜덤 요소 | 무시 | 치확·약확 100 이상에서 라이언 딜 두 판이 완전 동일 (8,491,764) |
| 딜 ∝ 공격력 | 실용상 통과 | 세팅 간 공격력 차이 보정 후 평균 일치 |
| 레이첼 패시브 약확 +27은 **자신 포함** | 확정 | 게임 내 확인 |

---

## 2. 스키마 추가

### 2.1 스킬별 히트 크기 (캐릭터 × 콘텐츠)
딜 흔들림은 히트 크기가 고르지 않을수록 커진다 (예: 라이언은 윗스킬 9방이 딜 대부분).
히트 크기는 공격력·보스·디버프에 따라 달라지므로 **콘텐츠 DB에 편성별로** 둔다.

```ts
type HitWeights = Record<string /*charId*/, Partial<Record<SkillSlot, number>>>;
// 값 = 치명·약공이 모두 안 터졌을 때의 한 히트 대미지 (후반, 디버프 최대 기준)
// 절대값보다 캐릭터 내 스킬 간 비율이 중요
```

### 2.2 전투 전용 장비 효과 (사용자 데이터)
마을 스탯에 표시되지 않고 전투 진입 시 붙는 효과 (반지 등).
```ts
type UserCharacter = {
  // ... v0.1 필드
  battleOnlyBonus?: { crit?: number; critDmg?: number; weakRate?: number };
  ring?: "damage" | "crit" | "weak" | "survival";
};
```

반지 종류:
- `damage`: 대상 1~3인 스킬 딜 +10% 표기. **실효 약 +4%** (커뮤니티 실측, 미검증). 배분 판단에는 영향 없음
- `crit`: 전투 치확 +10
- `weak`: 전투 약확 +12
- `survival`: 딜 영향 없음

### 2.3 스킬 고유 치피 보정
v0.1에서 무시했던 타카 아랫스킬 치피 +46은 히트 가중치 산출에 필요하므로 복원한다.
```ts
type Skill = { /* v0.1 필드 */ critDmgBonus?: number };
```

---

## 3. 시뮬레이터

### 3.1 한 판 시뮬레이션
캐릭터 c의 모든 히트 h에 대해:
```
대미지_h = 기본값_h × 치명항 × 약공항
치명항 = (rand < 실효치확) ? (치피 + critDmgBonus_h) / 100 : 1
약공항 = (rand < 실효약확) ? 1.3 : 1
```
- 히트 목록: 스킬 순서 × 대상 수 × hitsPerTarget + 평타 (totalTurns ÷ 유닛 수)
- 평타 수가 소수면 (예: 8.5) 판마다 8 또는 9를 반반 확률로
- 실효 스탯 = min(100, 마을 + 전투 버프 + battleOnlyBonus)

### 3.2 캐릭터별 보정 (스케일)
기본값의 절대 크기는 초반 디버프 등으로 어긋나므로, **캐릭터별 스케일 계수**를 실측 평균에 맞춰 보정한다.
```
scale_c = 실측 평균 딜_c / 시뮬레이션 평균 딜_c (보정 전)
```
스케일은 배분이 바뀌어도 고정. 모델은 배분에 따른 **상대 변화**와 **흔들림**을 계산하는 역할.

### 3.3 출력
N판(기본 20,000) 반복 후 캐릭터별·합계:
- 평균, 표준편차(절대·상대)
- 베스트1/5 기대값: 5판 묶음의 최대값 평균 (채점 방식 `bestOf 5`)
- 분포 히스토그램 데이터

### 3.4 성능
20,000판 × 약 100히트 = 200만 난수. 메인 스레드에서 1초 이내 목표. 느리면 Web Worker로 이동.
난수는 시드 고정 가능해야 한다 (테스트 재현성).

---

## 4. 금요일 공성전 추가 데이터

### 4.1 히트 크기 (후반 실측값 → 기본값 환산)
실측값은 치명+약공이 모두 터진 숫자. 측정 당시 장비(세팅 1·2와 다름):
라이언 공격력 5,150 / 치피 270, 타카 공격력 5,015 / 치피 258, 레이첼 세팅 1·2와 동일(치피 246).
기본값 = 실측값 ÷ ((치피 + critDmgBonus)/100 × 1.3)

| 캐릭터 | 스킬 | 실측 (치명+약공) | 기본값 |
|---|---|---|---|
| 라이언 | 윗 | 748,474 | 213,240 |
| 라이언 | 아랫 | 114,396 | 32,591 |
| 라이언 | 평타 | 61,329 | 17,473 |
| 타카 | 윗 | 511,505 | 152,506 |
| 타카 | 아랫 (치피 +46) | 193,766 | 49,030 |
| 타카 | 평타 | 94,313 | 28,120 |
| 레이첼 | 윗 | 77,516 | 24,239 |
| 레이첼 | 아랫 (1타당) | 59,106 | 18,475 |
| 레이첼 | 평타 | 30,004 (23,080×1.3) | 9,382 |

지크·비스킷: 딜 비중 1% 미만 → 평타 가중치 1로 두고 스케일만 보정.

### 4.2 타카 디버프 (참고, v0.2 미구현)
타카 공격 2회마다 적이 받는 물리 피해 +8% (최대 32%). 약 40턴 이후 최대.
v0.2는 후반 기준 가중치 + 스케일 보정으로 처리. 흔들림 오차가 문제되면 v0.3에서 턴별 스택 곡선 추가.

---

## 5. 검증 픽스처 (`tests/fixtures/siege-fri-validation.json`)

### 5.1 세팅별 전투 스탯 (마을 + 전투 버프 + 반지, 상한 적용 전)
| 캐릭터 | 세팅 | 공격력 | 치확 | 치피 | 약확 | 반지 |
|---|---|---|---|---|---|---|
| 라이언 | 1 | 5,325 | 100 | 258 | 20+81 | damage |
| 라이언 | 2 | 5,225 | 59 | 312 | 20+81 | damage |
| 타카 | 1 | 4,915 | 99 | 270 | 20+81 | damage |
| 타카 | 2 | 4,965 | 55 | 330 | 20+81 | damage |
| 레이첼 | 1·2 | 4,252 | 93+10 | 246 | 45+27 | crit |
| 지크 | 1·2 | 3,859 | 99 | 270 | 5+27 | survival |
| 비스킷 | 1·2 | 2,411 (마법) | 5 | 150 | 15+27 | survival |

### 5.2 결과 (캐릭터별 피해)
```json
{
  "setting1": [
    { "rachel": 1070309, "sieg": 171896, "biscuit": 4180, "ryan": 8491764, "taka": 7897649 },
    { "rachel": 1165667, "sieg": 166830, "biscuit": 4207, "ryan": 8490034, "taka": 7910047 },
    { "rachel": 1133834, "sieg": 161671, "biscuit": 4180, "ryan": 8491764, "taka": 7788597 }
  ],
  "setting2": [
    { "rachel": 1152754, "sieg": 162104, "biscuit": 4180, "ryan": 6127872, "taka": 6741081 },
    { "rachel": 1063638, "sieg": 185311, "biscuit": 4207, "ryan": 8242383, "taka": 6773105 },
    { "rachel": 1071444, "sieg": 183379, "biscuit": 4180, "ryan": 8862923, "taka": 7188984 },
    { "rachel": 1042705, "sieg": 158037, "biscuit": 4180, "ryan": 7330511, "taka": 5894139 },
    { "rachel": 1111795, "sieg": null,   "biscuit": 4180, "ryan": 6682581, "taka": 6629208 }
  ]
}
```
비고: setting2 5판째 지크는 공략 분기(지크 윗스킬 사용)로 제외.
결과 화면 행 순서: 레이첼, 지크, 비스킷, 라이언, 타카 (진형 순서와 다름).

### 5.3 테스트 기준
1. **스케일 보정**: 세팅 1 평균으로 라이언·타카·레이첼 스케일 산출
2. **평균 예측**: 그 스케일로 세팅 2를 시뮬레이션 → 라이언·타카 평균이 실측 평균의 **±5% 이내**
3. **흔들림**: 세팅 2 라이언 시뮬레이션 상대 표준편차가 **9~36%** 범위 (5판 표본의 90% 신뢰구간)
4. **결정성**: 세팅 1 라이언(치확·약확 100 이상)은 시뮬레이션 표준편차 0

---

## 6. 이번 작업 범위 (v0.2)

1. 2장 스키마 추가
2. 4장 데이터 반영 (`siege-fri.json`에 hitWeights, `users.json`에 ring·battleOnlyBonus)
3. `src/engine/simulate.ts`: 3장 시뮬레이터 (시드 고정 난수)
4. `src/engine/calibrate.ts`: 3.2 스케일 보정
5. 5장 픽스처 + 테스트
6. 간단한 화면: 캐릭터별 평균·표준편차·베스트1 기대값 표
7. CLAUDE.md에 1장 확정 규칙 추가

## 7. 미결 사항
- 레이첼 흔들림: 실측 4.2% vs 모델 약 2.5% (8판 기준 경계선). 표본 추가로 재확인
- 타카 디버프 스택 곡선 (4.2)
- 반지 실효값 실측 (현재 커뮤니티 값)
- 다음 단계: 장비 인벤토리 스키마 → 최적화기
